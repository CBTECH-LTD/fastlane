<span>
    @foreach ($value as $v)
        <span class="m-1 px-1 rounded border border-gray-300 bg-gray-200 text-sm text-gray-700 font-normal">
            {{ $v }}
        </span>
    @endforeach
</span>
